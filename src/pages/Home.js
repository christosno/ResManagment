import { Button } from 'react-daisyui';

const Home = () => {
    return (
        <div >
            Home
            <Button color="primary">Click me!</Button>
        </div>
    );
};

export default Home;
